import ParentCategoryModel from './ParentCategoryModel';

class SubCategoryModel extends ParentCategoryModel {

    isSelected = false

    constructor(props) {
        super(props)
        if (props) {
        }
    }

}

export default SubCategoryModel;